# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# 环境变量（含敏感信息）
.env.local

# 用户配置（config.example.yaml 是样例）
config.yaml

# 打包时由 build.sh 写入的 commit hash（dev 模式下从 git 读）
commit.txt
